def hello(name,nickname):
    print('Hello Python')
    print(f"Helo {name}")
    print(f"Helo {nickname}")
hello(name="Marukin",nickname = "Mix")

#----------------------------------------#

def sumall(num1,num2):
    sum = num1*num2
    print(sum)

a = 5
b = 10
sumall(a,b)

#-----------------------------------------#

def sumall(num1,num2):
    sum = num1*num2
    print(sum)

a = int(input('ใส่ค่า: '))
b = int(input('ใส่ค่า: '))
sumall(a,b)

#------------------------------------------#

def sumall(num1,num2): #ชื่อของฟังก์ชั่นและค่าที่ฟังก์ต้องการ
    sum = num1*num2 #หน้าที่ของฟังก์ชั่น
    return sum #ค่าของฟังก์ชั่น
    print(sum)

a = int(input('ใส่ค่า: '))
b = int(input('ใส่ค่า: '))
c = sumall(a,b)
print = (c)

#-------------------------------------------#

def sumall(num1,num2): #ชื่อของฟังก์ชั่นและค่าที่ฟังก์ต้องการ
    sum = num1*num2 #หน้าที่ของฟังก์ชั่น
    return sum #ค่าของฟังก์ชั่น


def sumall(num1,num2): #ชื่อของฟังก์ชั่นและค่าที่ฟังก์ต้องการ
    sum = num1/num2 #หน้าที่ของฟังก์ชั่น
    return sum #ค่าของฟังก์ชั่น
